package ru.yandex.practicum.filmorate;

import ru.yandex.practicum.filmorate.service.FilmService;
import ru.yandex.practicum.filmorate.model.Film;
import ru.yandex.practicum.filmorate.type.*;

import java.time.Duration;
import java.util.Date;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class Main {
    public static void main(String[] args) {
/*
        FilmService service =  new FilmService();
        List<Film> list =  service.getAllItems();
        Film film = new Film(null
                            , new FilmNameType( String.valueOf(list.size()+1 ))
                            , new FilmDescriptionType( "Descr "+String.valueOf(list.size()+1 ))
                            , new ReleaseDateType(new Date(2000, 1,1))
                            , // new DurationType( 60*100)
                Duration.ofMinutes(90)

        );
        System.out.println(list);
        System.out.println(film);
        System.out.println(service.create(film));
        Map<Long, String> map = new HashMap<>();
        for (int i=1; i<=3; i++) {
            map.put(Long.valueOf(i), String.valueOf(i));
        }
        System.out.println(map);
        Duration d = Duration.ofMinutes(90);


 */


    }

}
